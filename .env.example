# Optional. Evidence mode and the playground work without AI.
# OPENAI_API_KEY=

# Leave unset to use `bunx playwright install chromium`.
# Set to chrome if Google Chrome is installed locally.
# PLAYWRIGHT_CHANNEL=chrome

# Must match the local app server when using the built-in playground.
PLAYGROUND_ORIGIN=http://127.0.0.1:3040
